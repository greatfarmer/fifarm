spring:
  profiles:
    active: local
  devtools:
    livereload:
      enabled: true
  thymeleaf:
    cache: false

---
spring:
  profiles: local
  data:
    mongodb:
      host: localhost
      port: 27017
      database: fifarm
  jpa:
    show-sql: true

server:
  port: 8090

---
spring.profiles: production
spring.profiles.include: real-db

server:
  port: 8080